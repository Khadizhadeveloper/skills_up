[phases.setup]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[phases.build]

[start]
cmd = "python manage.py migrate && python setup_admin.py && gunicorn config.wsgi:application --bind 0.0.0.0:$PORT"
